import app from '../../app';
import request from 'supertest';

describe('Repor File API', () => {
  describe('GET /api/report-file', () => {
    it('shall return all report files', () => {
      expect(true).toBe(true);
    });
  });
});
